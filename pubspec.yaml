name: recipe_recommend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔥 Firebase (existing)
  firebase_auth: ^5.5.1
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5

  # 🌐 HTTP for Groq API calls
  http: ^0.13.6


  # 🔐 For loading API key securely
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # ✅ Add assets here
  assets:
    - assets/background_pattern.png
    - assets/chef_illustration.png
    - assets/3.png

